*.db
*.db-journal
*.db-wal
*.db-shm
/bin
/webcron
/webcron.exe
/.env
/build

# Compiled templates are generated at runtime; never commit them (stale
# compiled caches can shadow updated source templates).
frontend/templates/cache/*
!frontend/templates/cache/.gitkeep
