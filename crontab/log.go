package crontab

/*
// Log represents each log field
type Log struct {
	Name     string         `db:"name"`
	Output   types.JSONText `db:"output"`
	Stamp    time.Time      `db:"stamp"`
	Duration time.Duration  `db:"duration"`
}

type Logs []Log

func (l *Log) save(db *factory.DB) error {
	return db.Insert("logs", l)
}
*/
